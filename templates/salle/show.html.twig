{% extends 'base.html.twig' %}

{% block title %}Salle{% endblock %}

{% block main %}
<div class="container">
    <h1>Salle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salle.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ salle.name }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ salle.adresse }}</td>
            </tr>
            <tr>
                <th>UrlFacebook</th>
                <td>{{ salle.urlFacebook }}</td>
            </tr>
            <tr>
                <th>UrlSite</th>
                <td>{{ salle.urlSite }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ salle.img }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('salle_index') }}">back to list</a>

    <a href="{{ path('salle_edit', {'id': salle.id}) }}">edit</a>

    {{ include('salle/_delete_form.html.twig') }}
</div>
{% endblock %}
